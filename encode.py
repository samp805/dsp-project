def main():
    # input text
    text = raw_input("Input text:  ")
    chars = list(text)
    # timefreqs = map(parse_chars, chars)

    # chain together the timefreqs
    # output to a wav file

if __name__ == "__main__":
    main();
